{
    "$id": "kyruusProviders",
    "$schema": "https://schema.yext.com/config/platform/plugin/v1",
    "globals": {
        "clientId": "${{clientId}}",
        "clientSecret": "${{clientSecret}}",
        "customer": "${{customer}}"
    }  
}
